        - const _taxodist_cache = Dict{String,Any}()
        - 
        - const _rank_prefixes =
        -     "Clade|Kingdom|Phylum|Superphylum|Subphylum|Infraphylum|Class|Order|" *
        -     "Suborder|Infraorder|Parvorder|Grandorder|Magnorder|Cohort|Subcohort|" *
        -     "Legion|Family|Subfamily|Tribe|Subtribe|Genus|Species|Subkingdom|" *
        -     "Infrakingdom|Superclass|Subclass|Infraclass|Superorder|Superfamily|" *
        -     "Domain|Superkingdom|Grade|Subgrade|Supergrade"
        - 
        - const _bare_ranks = Set([
        -     "Go to", "Superphylum", "Subfamily", "Suborder", "Epifamily",
        -     "Infraorder", "Superclass", "Subclass", "Superfamily", "Subgenus",
        -     "Section", "Division", "Candidatus", "Parvphylum", "Branch",
        -     "Supercohort", "Infracohort", "Subdivision", "Subsection", "Grade",
        -     "[unranked]", "(Supercluster)", "(Region)", "[crown]", "",
        - ])
        - 
        - const _astronomical_pattern =
        -     r"\bastronomical\b|\bplanet\b|\bMinor planet\b|\bcomet\b|\bastronomy\b|\basteroid\b"i
        - 
        - const _user_agent = "taxodist Julia package/0.6.0"
        - const _http_get = Ref{Function}(HTTP.get)
        - 
      111 _normalise_space(text) = strip(replace(String(text), r"\s+" => " "))
        - 
        - const _regex_special_characters = Set(
        -     ['\\', '.', '^', '$', '*', '+', '?', '{', '}', '[', ']', '|', '(', ')'],
        - )
        - 
       18 function _regex_escape(text::AbstractString)
       18     output = IOBuffer()
       36     for character in String(text)
      296         character in _regex_special_characters && write(output, '\\')
      148         write(output, character)
      278     end
       18     return String(take!(output))
        - end
        - 
       18 function _contains_distinct_word(text::AbstractString, word::AbstractString)
       18     isempty(word) && return false
       18     pattern = Regex("\\b$(_regex_escape(word))\\b", "i")
       18     return occursin(pattern, text)
        - end
        - 
        - const _text_break_tags = Set([
        -     :br, :div, :p, :li, :tr, :td, :th, :table, :section, :article,
        -     :header, :footer, :h1, :h2, :h3, :h4, :h5, :h6,
        - ])
        - 
        7 function _write_structured_text(io::IO, node::Gumbo.HTMLText)
        7     write(io, node.text)
        - end
        - 
        9 function _write_structured_text(io::IO, node::Gumbo.HTMLElement)
        9     node_tag = Gumbo.tag(node)
       18     node_tag in _text_break_tags && write(io, '\n')
        9     for child in Gumbo.children(node)
       23         _write_structured_text(io, child)
       15     end
        9     node_tag in _text_break_tags && write(io, '\n')
        - end
        - 
        1 _write_structured_text(io::IO, node::Gumbo.HTMLNode) = nothing
        - 
        - function _structured_text_lines(node::Gumbo.HTMLNode)
        2     text = sprint(io -> _write_structured_text(io, node))
        1     lines = [_normalise_space(line) for line in split(text, '\n')]
       18     return filter(line -> !isempty(line), lines)
        - end
        - 
       48 function _clean_lineage_label(raw_text)
       48     text = replace(String(raw_text), r"[†ᵀ]" => "")
       48     text = _normalise_space(text)
       48     text = replace(text, Regex("^\\[crown\\]\\s+(" * _rank_prefixes * ")?\\s*") => "")
       48     text = replace(text, Regex("^(" * _rank_prefixes * ") ") => "")
       48     text = replace(text, r"\s+[A-Z][a-záàâãéèêíïóôõöúüç].*$" => "")
       48     text = replace(text, r"\s+[A-Z]\.[A-Z]\..*$" => "")
       48     text = replace(text, r"\s+auct\..*$" => "")
       48     text = replace(text, r"\s+von.*$" => "")
       48     text = replace(text, r"\s+\([A-Z][a-z].*$" => "")
       48     text = replace(text, r"\s+\(\d{4}\).*$" => "")
       48     text = replace(text, r"\s+\[.*$" => "")
       48     text = replace(text, r"\s+[A-Z]\.$" => "")
       48     text = replace(text, r"\s+\([a-z].*$" => "")
       48     text = replace(text, r"\s+\".*$" => "")
       48     text = replace(text, r"^\".*" => "")
       48     return strip(text)
        - end
        - 
       30 function clear_cache()
     1633     empty!(_taxodist_cache)
       31     return nothing
        - end
        - 
        - function save_cache(file::AbstractString)
        1     open(file, "w") do stream
        1         JSON3.write(stream, _taxodist_cache)
        -     end
        1     println("Cache saved to '$(file)' ($(length(_taxodist_cache)) entries).")
        1     return nothing
        - end
        - 
        9 function _validated_cache(raw)
        9     data = Dict{String,Any}()
        - 
       18     for (raw_key, raw_value) in pairs(raw)
       10         key = String(raw_key)
       10         isempty(key) && throw(ArgumentError(
        -             "Invalid cache file: cache keys must be non-empty strings.",
        -         ))
        - 
        9         if startswith(key, "id_")
        3             value = raw_value isa AbstractString ? String(raw_value) : nothing
        3             (value === nothing || isempty(value)) && throw(ArgumentError(
        -                 "Invalid cache file: taxon ID entries must be non-empty strings.",
        -             ))
        1             data[key] = value
        6         elseif startswith(key, "lin_")
        3             raw_value isa AbstractVector || throw(ArgumentError(
        -                 "Invalid cache file: lineage entries must be sequences of strings.",
        -             ))
        8             all(node -> node isa AbstractString, raw_value) || throw(ArgumentError(
        -                 "Invalid cache file: lineage entries must be sequences of strings.",
        -             ))
        1             data[key] = String.(collect(raw_value))
        3         elseif startswith(key, "resolved_lineage_")
        2             raw_value isa AbstractVector || throw(ArgumentError(
        -                 "Invalid cache file: resolved lineage entries must be sequences of strings.",
        -             ))
        8             all(node -> node isa AbstractString, raw_value) || throw(ArgumentError(
        -                 "Invalid cache file: resolved lineage entries must be sequences of strings.",
        -             ))
        1             data[key] = String.(collect(raw_value))
        -         else
        1             data[key] = raw_value
        -         end
        5     end
        - 
        3     return data
        - end
        - 
        4 function load_cache(file::AbstractString)
        5     isfile(file) || throw(ArgumentError("Cache file not found: '$(file)'"))
        3     raw = JSON3.read(read(file, String))
        4     raw isa JSON3.Object || throw(ArgumentError(
        -         "Invalid cache file: expected an object created by save_cache().",
        -     ))
        - 
        2     data = _validated_cache(raw)
        1     merge!(_taxodist_cache, data)
        1     println("Cache loaded from '$(file)' ($(length(data)) entries).")
        1     return nothing
        - end
        - 
        2 function cache_info()
        2     lineage_keys = sort([key for key in keys(_taxodist_cache) if startswith(key, "lin_")])
        2     id_keys = sort([key for key in keys(_taxodist_cache) if startswith(key, "id_")])
        4     taxa = replace.(lineage_keys, r"^lin_" => "")
        2     size_bytes = Base.summarysize(_taxodist_cache)
        - 
        2     println("taxodist Cache")
        2     println("* Lineages cached : $(length(lineage_keys))")
        2     println("* IDs cached      : $(length(id_keys))")
        2     println("* Memory used     : $(size_bytes) bytes")
        - 
        2     if isempty(taxa)
        1         println("\nNo lineages cached yet.")
        -     else
        1         println("\nCached taxa:")
        2         foreach(taxon -> println("  $(taxon)"), taxa)
        -     end
        - 
        2     return (
        -         n_lineages=length(lineage_keys),
        -         n_ids=length(id_keys),
        -         taxa=taxa,
        -         size_bytes=size_bytes,
        -     )
        - end
        - 
       38 function _request_html(url::AbstractString; verbose::Bool=false)
       19     try
       19         response = _http_get[](
        -             url,
        -             ["User-Agent" => _user_agent];
        -             status_exception=false,
        -             retry=false,
        -             request_timeout=30,
        -         )
       18         if response.status != 200
        2             verbose && println("Could not reach Taxonomicon (HTTP $(response.status)).")
        2             return nothing
        -         end
       16         return String(response.body)
        -     catch error
        1         verbose && println("Could not reach Taxonomicon: $(sprint(showerror, error))")
        1         return nothing
        -     end
        - end
        - 
       15 function _valid_tree_link(row)
       15     for link in eachmatch(Selector("a.Valid"), row)
       26         href = Gumbo.getattr(link, "href", "")
       13         occursin("TaxonTree", href) || continue
       12         match_id = match(r"id=([0-9]+)", href)
       23         match_id === nothing || return (id=match_id.captures[1], href=href)
        2     end
        4     return nothing
        - end
        - 
       11 function _parse_search_results(html::AbstractString)
       11     document = parsehtml(String(html))
       11     results = NamedTuple{(:id, :name),Tuple{String,String}}[]
       11     seen = Set{String}()
        - 
       11     for row in eachmatch(Selector("tr"), document.root)
       16         text_entry = _normalise_space(nodeText(row))
       16         occursin(_astronomical_pattern, text_entry) && continue
       15         tree_link = _valid_tree_link(row)
       15         tree_link === nothing && continue
       11         tree_link.id in seen && continue
        - 
       10         text_entry = replace(
        -             text_entry,
        -             r"^N\s*\|\s*T\s*\|\s*P\s*\|\s*R\s*\|\s*B\s*\|\s*L\s*" => "",
        -         )
       20         push!(results, (id=tree_link.id, name=text_entry))
       10         push!(seen, tree_link.id)
       16     end
        - 
       11     return results
        - end
        - 
       16 function taxo_search(taxon; verbose::Bool=false)
        8     verbose && println("Searching Taxonomicon for '$(taxon)'...")
        8     safe_taxon = HTTP.URIs.escapeuri(String(taxon))
       16     url = "http://taxonomicon.taxonomy.nl/TaxonList.aspx?" *
        -           "subject=Entity&by=ScientificName&search=$(safe_taxon)"
        8     html = _request_html(url; verbose=verbose)
        8     html === nothing && return nothing
        - 
        7     parsed = _parse_search_results(html)
        7     if isempty(parsed)
        2         verbose && println("No matches found.")
        2         return nothing
        -     end
        - 
        5     results = DataFrame(parsed)
        5     verbose && println("Found $(nrow(results)) entries.")
        5     return results
        - end
        - 
       24 function _parse_lineage_html(html::AbstractString, taxon_id::AbstractString; clean::Bool=true)
       12     document = parsehtml(String(html))
       12     subject_nodes = eachmatch(Selector("#ctl00_divSubject b"), document.root)
       12     content_nodes = eachmatch(Selector("#divPageContent"), document.root)
       12     texts = String[]
        - 
        -     # Match the R/Python parser: prefer the complete hierarchy text because
        -     # The Taxonomicon includes meaningful intermediate nodes that are not
        -     # always hyperlinks. Stop at the page's subject taxon so descendants are
        -     # never included.
       12     if !isempty(subject_nodes) && !isempty(content_nodes)
        1         current_name = _normalise_space(nodeText(subject_nodes[1]))
        1         raw_lines = _structured_text_lines(content_nodes[1])
        - 
        3         tree_start = findfirst(line -> startswith(line, "Natura"), raw_lines)
        2         tree_start === nothing || (raw_lines = raw_lines[tree_start:end])
        - 
        1         searchable_lines = replace.(raw_lines, r"[†ᵀ]" => "")
        7         cutoff = findfirst(
        6             line -> _contains_distinct_word(line, current_name),
        -             searchable_lines,
        -         )
        2         texts = cutoff === nothing ? raw_lines : raw_lines[1:cutoff]
        -     else
        -         # Fallback for simplified or legacy pages without the content IDs.
       11         links = eachmatch(Selector("a"), document.root)
       11         for link in links
       60             href = Gumbo.getattr(link, "href", "")
       30             occursin("TaxonTree", href) || continue
       30             occursin(r"id=[0-9]+", href) || continue
       28             push!(texts, _normalise_space(nodeText(link)))
       28             link_id = match(r"id=([0-9]+)", href)
       28             link_id !== nothing && link_id.captures[1] == taxon_id && break
       22         end
        -     end
        - 
       12     lineage = String[]
       12     seen = Set{String}()
       12     for raw_text in texts
       62         label = _clean_lineage_label(raw_text)
       34         label in _bare_ranks && continue
       33         isempty(label) && continue
       33         startswith(label, "\"") && continue
       33         startswith(label, "Population") && continue
       32         label in seen && continue
       31         push!(lineage, label)
       31         push!(seen, label)
       34     end
        - 
       12     if clean
       22         biota_index = findfirst(==("Biota"), lineage)
       18         biota_index === nothing || (lineage = lineage[biota_index:end])
        -     end
        - 
       12     return lineage
        - end
        - 
       94 function get_lineage_by_id(taxon_id; clean::Bool=true, verbose::Bool=false)
       47     id = strip(String(taxon_id))
       47     isempty(id) && return nothing
       47     occursin(r"^[0-9]+$", id) || return nothing
        - 
       45     cache_key = "lin_$(id)"
       85     if haskey(_taxodist_cache, cache_key)
       37         verbose && println("Using cached lineage for ID $(id)")
       37         return copy(_taxodist_cache[cache_key])
        -     end
        - 
        8     url = "http://taxonomicon.taxonomy.nl/TaxonTree.aspx?id=$(id)&src=0"
        8     html = _request_html(url; verbose=verbose)
        8     html === nothing && return nothing
        - 
        8     lineage = _parse_lineage_html(html, id; clean=clean)
        8     isempty(lineage) && return nothing
        5     _taxodist_cache[cache_key] = lineage
        5     return copy(lineage)
        - end
        - 
      100 function get_taxonomicon_id(taxon; verbose::Bool=false)
       50     taxon_name = String(taxon)
       50     cache_key = "id_$(taxon_name)"
       95     if haskey(_taxodist_cache, cache_key)
       45         verbose && println("Using cached ID for $(taxon_name)")
       45         return _taxodist_cache[cache_key]
        -     end
        - 
        5     candidates = taxo_search(taxon_name; verbose=verbose)
        5     candidates === nothing && return nothing
        - 
        4     biological = NamedTuple[]
        8     for candidate in eachrow(candidates)
        6         lineage = get_lineage_by_id(candidate.id; clean=true, verbose=false)
        6         lineage === nothing && continue
        5         "Biota" in lineage || continue
        4         push!(biological, (id=String(candidate.id), name=String(candidate.name)))
        8     end
        - 
        4     isempty(biological) && return nothing
        - 
        2     if length(biological) > 1
        2         exact = [candidate for candidate in biological if begin
        4             lineage = get_lineage_by_id(candidate.id; clean=true, verbose=false)
        4             lineage !== nothing && any(
       10                 node -> _contains_distinct_word(node, taxon_name), lineage,
        -             )
        -         end]
        2         isempty(exact) || (biological = exact)
        -     end
        - 
        2     if length(biological) > 1
        1         @warn "Multiple valid biological entries found for '$(taxon_name)'. Using ID $(biological[1].id). Pass a numeric ID directly to select another entry."
        -     end
        - 
        2     final_id = biological[1].id
        2     _taxodist_cache[cache_key] = final_id
        2     verbose && println("Found $(taxon_name) with ID $(final_id)")
        2     return final_id
        - end
        - 
      164 function get_lineage(taxon; clean::Bool=true, verbose::Bool=false)
       82     taxon_name = String(taxon)
       82     is_id = occursin(r"^[0-9]+$", taxon_name)
       82     resolved_key = "resolved_lineage_$(clean ? 1 : 0)_$(taxon_name)"
      158     if !is_id && haskey(_taxodist_cache, resolved_key)
       34         verbose && println("Using cached resolved lineage for $(taxon_name)")
       34         return copy(_taxodist_cache[resolved_key])
        -     end
       90     id = is_id ? taxon_name : get_taxonomicon_id(taxon_name; verbose=verbose)
       48     id === nothing && return nothing
        - 
       31     lineage = get_lineage_by_id(id; clean=clean, verbose=verbose)
       31     lineage === nothing && return nothing
       30     is_id && return lineage
        - 
       24     if !occursin(r"\s", taxon_name)
       23         lineage = [node for node in lineage if !occursin(" ", node) && !startswith(node, "[")]
       23         target_index = findlast(==(taxon_name), lineage)
        -     else
        1         lineage = [node for node in lineage if !occursin(" ", node) || node == taxon_name]
        1         target_index = findfirst(==(taxon_name), lineage)
        -     end
        - 
       24     if target_index === nothing
        2         push!(lineage, taxon_name)
        -     else
       23         lineage = lineage[1:target_index]
        -     end
        - 
       24     isempty(lineage) && return nothing
       24     _taxodist_cache[resolved_key] = copy(lineage)
       24     return lineage
        - end
